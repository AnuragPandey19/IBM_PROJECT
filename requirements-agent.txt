# Extra dependencies for the test-case-generation agent
# Install with: pip install -r requirements-agent.txt

pyyaml>=6.0             # config parsing
python-dotenv>=1.0      # loads .env for API keys
requests>=2.31          # backend calls + Ollama HTTP

# LLM SDKs — install only the one(s) you use, but pip-installing all is harmless
openai>=1.35            # covers OpenAI, Groq, Ollama, together, any OpenAI-compatible API
anthropic>=0.34         # Claude
google-genai>=0.3       # Gemini
